server:
  port: 10086 #服务端口号

spring:
  application:
    name: eurekaserver

eureka:
  client:
    service-url: #eureka服务的地址信息
      defaultZone: http://127.0.0.1:10086/eureka
    fetch-registry: false